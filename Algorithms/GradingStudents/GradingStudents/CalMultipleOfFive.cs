reference object, instance Vs class

var quotient = grades[i] / 5;
                        Console.WriteLine(quotient);
                        var nextFifth = (quotient + 1) * 5;
                        Console.WriteLine(nextFifth);
                        finalGrade = nextFifth;
                        Console.WriteLine(finalGrade);